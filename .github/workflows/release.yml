name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Release
        run: |
          xcodebuild \
            -project caffeine.xcodeproj \
            -scheme caffeine \
            -configuration Release \
            -derivedDataPath ./build \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Create DMG
        run: |
          APP_PATH="./build/Build/Products/Release/caffeine.app"
          DMG_PATH="Caffeine.dmg"
          hdiutil create -volname "Caffeine" -srcfolder "$APP_PATH" -ov -format UDZO "$DMG_PATH"

      - name: Generate Checksum
        run: shasum -a 256 Caffeine.dmg > Caffeine.dmg.sha256

      - name: Upload DMG as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Caffeine.dmg
          path: |
            Caffeine.dmg
            Caffeine.dmg.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: Caffeine.dmg

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Caffeine.dmg
            Caffeine.dmg.sha256
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
